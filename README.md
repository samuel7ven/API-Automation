# API Automation with Python and Docker

This project demonstrates how to automate API requests using Python and Docker. It fetches posts from a public JSONPlaceholder API and displays the results.

## Getting Started

### Prerequisites
- Docker
- Python 3.x (if running locally)

### Setting Up Locally

1. Clone the repo:
   ```bash
   git clone https://github.com/samuel7ven/API-Automation.git
   cd API-Automation
